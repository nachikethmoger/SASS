Sass is a CSS pre-processor.

Sass reduces repetition of CSS and therefore saves time

other css processor are less, stylus, Sass

it is designed it an such a way that it acts as a programming language
it has if else statements, conditionals, loops